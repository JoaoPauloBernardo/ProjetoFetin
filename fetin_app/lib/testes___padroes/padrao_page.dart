import 'package:flutter/material.dart';

class NOMEARQUIVO extends StatefulWidget {
  const NOMEARQUIVO({super.key});

  @override
  State<NOMEARQUIVO> createState() => _NOMEARQUIVOState();
}

class _NOMEARQUIVOState extends State<NOMEARQUIVO> {
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
        body: Center(
            child: Stack(alignment: Alignment.center, children: [

              //code

          ]
        )
      )
    );
  }
}